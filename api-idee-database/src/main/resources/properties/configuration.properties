# controls
controls=${controls}
# services
services=${services}
# theme
themes=${theme.names}
# projection
projection=${api-idee.proj.default}
# api-idee url
api-idee.url=${api-idee.url}
# api-idee theme url
api-idee.theme.url=${api-idee.theme.url}
# max image size
max.image.size=1048576
# Proxy host (if was needed)
proxy.host=${proxy.host}
# Proxy port (if was need)
proxy.port=${proxy.port}